{
  "name": "drawio-lei-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "CLI helpers for generating, validating, and exporting draw.io network and DevOps diagrams.",
  "type": "module",
  "scripts": {
    "test": "node --test \"**/*.test.js\"",
    "cli": "node cli.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
